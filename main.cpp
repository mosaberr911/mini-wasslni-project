#include <iostream>

#include "ConsoleApp.h"
#include "Graph.h"
//#include "bits/stdc++.h"
int main() {
    ConsoleApp console;

    // graph.findallpaths("cairo", "cairo");
    /*
     * distance between city & itself
     * no exception when a city doesn't exist
     * no exception when no path between the cities
    */
    return 0;
}